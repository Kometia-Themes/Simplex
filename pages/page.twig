{% extends 'layout.twig' %}

{% block body %}
<div class="row">
  <div class="col-sm-12 col-md-8 col-md-offset-2">
    <h1 class="section-title">{{ page.title }}</h1>
    {{ page.content }}
  </div>
</div>
{% endblock %}
