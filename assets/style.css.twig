body {
  color: {{ settings.body_font_color }};
  background-color: {{ settings.shop_bg_color }};
  font-size: {{ settings.body_font_size }};
  font-family: {{ settings.base_font | remove('Google+') | replace({'+': ' '})}};
  line-height: 1.6;
}

h1, h2, h3, h4, h5, h6 {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  color: {{ settings.title_color }};
}

h1 {
  font-size: {{ settings.title_font_size }};
}

hr {
  clear: both;
  border-top: solid {{ settings.border_color }};
  border-width: 1px 0 0;
  margin: 50px 0;
  height: 0;
}

a {
  color: {{ settings.link_color }};
}

a:focus {
  outline: none;
  outline-offset: 0;
}

a:hover {
  color: {{ settings.link_color }};
  opacity: 0.7;
  -webkit-backface-visibility: hidden;
}

a:focus,
a:hover {
  text-decoration: none;
}

a.active {
  color: {{ settings.link_color }};
  font-weight: bold;
}

button,
input[type='submit'] {
  background: {{ settings.link_color }};
  color: {{ settings.button_text_color }};
}

input[type='text'],
input[type='email'],
input[type='tel'],
input[type='url'],
input[type='date'],
input[type='time'],
input[type='month'],
input[type='datetime'],
input[type='datetime-local'],
input[type='color'],
input[type='range'],
input[type='number'],
input[type='search'] {
  border: 1px solid {{ settings.border_color }};
}

label,
legend,
.label {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
}

sup {
  margin-left: 2px;
}

/* * * * * * * * */
/* Global styles */
/* * * * * * * * */

@media (min-width: 1200px) {
  .container {
    width: 970px;
  }
}

a.btn,
input.btn,
button.btn {
  background: {{ settings.link_color }};
  border-radius: 2px;
  color: {{ settings.button_text_color }};
  font-size: {{ settings.accent_font_size }};
  font-family: {{ settings.accent_font | remove('Google+') | replace({'+': ' '}) }};
  text-transform: none;
}

input.btn,
button.btn {
  text-transform: uppercase;
}

a.btn:hover,
input.btn:hover,
button.btn:hover {
  background-color: {{ settings.link_color }};
  color: #fff;
  opacity: 0.8;
  -webkit-backface-visibility: hidden;
}

input.disabled.btn:hover,
button.disabled.btn:hover {
  background-color: {{ settings.link_color }};
  color:#fff;
}

a,
button {
  outline: none;
  moz-transition: all 0.5s ease, outline 0s;
  webkit-transition: all 0.5s ease, outline 0s;
  -webkit-transition: all 0.5s ease, outline 0.000001s;
  transition: all 0.5s ease, outline 0.000001s;
}

.lead {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
}

@media screen and (min-width:992px) {
  .hidden-desktop {
    display:none !important;
  }
  .hidden-mobile {
    display:block !important;
  }
}

@media screen and (max-width: 991px) {
  .hidden-desktop {
    display:block !important;
  }
  .hidden-mobile {
    display:none !important;
  }
}

.placeholder-section {
  position: relative;
  clear: both;
}

.placeholder-section:hover .placeholder-content {
  opacity: 0.2;
  filter: alpha(opacity=20);
  transition: opacity 0.3s cubic-bezier(0, 0, 0.33, 1);
}

.placeholder-section:hover .placeholder-helper {
  opacity: 1;
}

.placeholder-helper {
  position: absolute;
  top: 50%;
  left: 50%;
  margin-top: -80px;
  margin-left: -265px;
  max-width: 510px;
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  font-size: 14px;
  background: #31373d;
  border: none;
  color: #96a7b6;
  text-align: left;
  padding: 30px;
  z-index: 99999;
  opacity: 0;
  -webkit-font-smoothing: initial;
  filter: alpha(opacity=0);
  -webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  -webkit-transform: translateZ(0);
  transform: translateZ(0);
  box-sizing: border-box;
  transition: opacity 0.3s cubic-bezier(0, 0, 0.33, 1),-webkit-transform 0.5s cubic-bezier(0.45, 0, 0, 0.95);
  transition: opacity 0.3s cubic-bezier(0, 0, 0.33, 1),transform 0.5s cubic-bezier(0.45, 0, 0, 0.95);
  transition: opacity 0.3s cubic-bezier(0, 0, 0.33, 1),transform 0.5s cubic-bezier(0.45, 0, 0, 0.95),-webkit-transform 0.5s cubic-bezier(0.45, 0, 0, 0.95);
}

.placeholder-section .placeholder-helper .placeholder-info p,
.placeholder-section .placeholder-helper .placeholder-info h3 {
  color: white;
}

.divider {
  width: 70%;
  margin: 50px auto;
  max-width: 360px;
}

.section-title {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  text-align: center;
  margin-bottom: 1em;
}

.title,
a.title:link,
a.title:hover,
a.title:active,
a.title:visited {
  color: {{ settings.title_color }};
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  font-size: {{ settings.title_font_size }};
}

.subtitle,
a.subtitle:link,
a.subtitle:hover,
a.subtitle:active,
a.subtitle:visited {
  color: {{ settings.subtitle_color }};
  font-size: {{ settings.accent_font_size }};
}

.ribbon {
  position: absolute;
  right: 15px;
  top: 0;
  z-index: 1;
  overflow: hidden;
  width: 75px; height: 75px;
  text-align: center;
}

.ribbon span {
  font-size: 10px;
  font-weight: bold;
  text-transform: uppercase;
  text-align: center;
  line-height: 20px;
  transform: rotate(45deg);
  -webkit-transform: rotate(45deg);
  width: 100px;
  display: block;
  position: absolute;
  top: 19px;
  right: -21px;
}

.ribbon span.danger {
  color: #fff;
  background: #f70505;
}

.ribbon span.warning {
  color: #fff;
  background: #f7c205;
}

/* * * * * * * */
/* Form styles */
/* * * * * * * */

.form-control {
  box-shadow: none;
}

.checkbox-control {
  padding-left: 20px;
}

.checkbox-control__input {
  opacity: 0;
  z-index: 1;
}

.checkbox-control__label {
  margin-top: 0;
  display: inline-block;
  vertical-align: middle;
  position: relative;
  padding-left: 5px;
}

.checkbox-control__label::before {
  content: "";
  display: inline-block;
  position: absolute;
  width: 17px;
  height: 17px;
  left: 0;
  margin-left: -20px;
  border: 1px solid #cccccc;
  border-radius: 0;
  background-color: #fff;
  -webkit-transition: border 0.15s ease-in-out, color 0.15s ease-in-out;
  -o-transition: border 0.15s ease-in-out, color 0.15s ease-in-out;
  transition: border 0.15s ease-in-out, color 0.15s ease-in-out;
}

.checkbox-control__label::after {
  display: inline-block;
  position: absolute;
  width: 16px;
  height: 16px;
  left: 0;
  top: 0;
  margin-left: -20px;
  padding-left: 3px;
  padding-top: 1px;
  font-size: 11px;
  color: #555555;
}
input.checkbox-control__input[type="checkbox"]:focus + label::before {
  outline: thin dotted;
  outline: 5px auto -webkit-focus-ring-color;
  outline-offset: -2px;
}
input.checkbox-control__input[type="checkbox"]:checked + label::after {
  font-family: "MaterialDesignIcons";
  content: "\f1fe";
  color: #999;
}
input.checkbox-control__input[type="checkbox"]:disabled + label {
  opacity: 0.65;
}
input.checkbox-control__input[type="checkbox"]:disabled + label::before {
  background-color: #eeeeee;
  cursor: not-allowed;
}

.checkbox-control__label--circle::before {
  border-radius: 50%;
}

/* * * * * * * * */
/* Layout styles */
/* * * * * * * * */

.header {
  margin-bottom: 20px;
}

.header a {
  color: {{ settings.title_color }};
}

.header__title img {
  margin: 0 auto;
}

.header__site {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  padding: 25px 0;
}

.v-align {
  float: none;
  display: inline-block;
  vertical-align: middle;
  margin-right: -4px;
}

@media (max-width: 767px) {
  .v-align {
    display: block;
  }
}

.header__title a {
  font-size: 58px;
  text-decoration: none;
}

.navbar-default {
  font-family: {{ settings.accent_font | remove('Google+') | replace({'+': ' '}) }};
  background: {{ settings.nav_bg_color }};
  border: none;
  border-radius: 3px;
}

.navbar-header {
  border-radius: 3px;
}

@media (min-width: 768px) {
  .navbar .navbar-nav {
      display: inline-block;
      float: none;
      vertical-align: top;
  }

  .navbar .navbar-collapse {
      text-align: center;
  }
}

.navbar-default .navbar-nav>li>a {
  color: {{ settings.nav_link_color }};
  font-size: {{ settings.accent_font_size }};
  font-weight: bold;
}

@media (max-width: 767px) {
  .navbar-default .navbar-nav .open .dropdown-menu>li>a {
    color: {{ settings.nav_link_color }};
    font-size: {{ settings.accent_font_size }};
  }
}

.navbar-toggle {
  margin-left: 15px;
  margin-right: 0;
}

.navbar-default .navbar-brand {
  color: {{ settings.nav_link_color }};
}

.navbar-default .navbar-toggle .icon-bar {
  background-color: {{ settings.nav_link_color }};
}

.dropdown-submenu {
  position: relative;
}

.dropdown-submenu>.dropdown-menu {
  top: 0;
  left: 100%;
  margin-top: -6px;
  margin-left: -1px;
  -webkit-border-radius: 0 6px 6px 6px;
  -moz-border-radius: 0 6px 6px;
  border-radius: 0 6px 6px 6px;
}

.dropdown-submenu.active>.dropdown-menu {
  display: block;
}

.dropdown-submenu.active>a:after {
  border-left-color: #fff;
}

.dropdown-submenu>a:after {
  display: block;
  content: " ";
  float: right;
  width: 0;
  height: 0;
  border-color: transparent;
  border-style: solid;
  border-width: 5px 0 5px 5px;
  border-left-color: #ccc;
  margin-top: 5px;
  margin-right: -10px;
}

.dropdown-submenu:hover>a:after {
  border-left-color: #fff;
}

.dropdown-submenu.pull-left {
  float: none;
}

.dropdown-submenu.pull-left>.dropdown-menu {
  left: -100%;
  margin-left: 10px;
  -webkit-border-radius: 6px 0 6px 6px;
  -moz-border-radius: 6px 0 6px 6px;
  border-radius: 6px 0 6px 6px;
}

.footer {
  padding-bottom: 50px;
}

.footer li {
  margin-bottom: 18px;
  font-weight: 400;
}

.footer__category {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  font-size: {{ settings.accent_font_size }};
  text-transform: uppercase;
  margin-bottom: 20px;
  font-weight: bold;
}

.footer__category-link {}
.footer__category-link:hover {}
.footer__payment-methods li {
  padding: 0 2px;
}

.footer__payment-methods img {
  width: 30px;
}

.footer__copyright {
  margin-bottom: 22px;
}

.footer__subscribe-input {
  font-weight: 400;
  border: 1px solid {{ settings.border_color }};
  height: 40px;
}

.footer__subscribe-button {
  font-size: 15px;
  height: 40px !important;
  text-transform: uppercase;
}

/* * * * * * * * * */
/* Product styles  */
/* * * * * * * * * */

.product__carousel {
  margin-top: 30px;
}

.product__carousel > .row {
  margin-bottom: 15px;
}

.product__carousel__thumb img {
  max-width: 150px;
}

.product-card {
  display: block;
  transition: border .2s ease-in-out;
  -webkit-transition: border .2s ease-in-out;
  -o-transition: border .2s ease-in-out;
  margin-bottom: 30px;
}

.product-card__details {
  text-align: center;
  padding: 5px 0;
}

.product-card__details:hover {
  text-decoration: none;
}

.product-card__name {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  font-size: 18px;
  color: {{ settings.body_font_color }};
}

.product-card__price {
  color: {{ settings.price_color }};
}

.product-card__price del {
  color: {{ settings.discount_color }};
}

.products-filter__title {
  margin-top: 25px;
  text-transform: uppercase;
}

.product-single__pics {
  margin-bottom: 25px;
}

.product-single__name {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  color: {{ settings.title_color }};
}

.product-single__description p {
  margin-bottom: 25px;
}

.product-single__price {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  font-size: 1.73333em;
  margin-bottom: 0;
  margin-top: 5px;
  color: {{ settings.subtitle_color }};
  opacity: 0.7;
}

@media screen and (min-width: 480px) {
  .product-single__price {
    margin-right: 25px;
    margin-left: 40px;
  }
}

.product-single__price del {
  font-size: {{ settings.body_font_size }};
  display: block;
}

.product-single label {
  padding: 0 20px 0 10px;
}

.product-single__social {
  margin-top: 30px;
}

.product-single__qty input[type="number"] {
  max-width: 60px;
  text-align: center;
  margin: 0 auto;
}

/* * * * * * * * * */
/* Category styles */
/* * * * * * * * * */

ul.category li a {
  color: {{ settings.main_menu_link_color }};
}

/* * * * * * * * * * * * */
/* Blogs / posts styles  */
/* * * * * * * * * * * * */

.blog-posts__header {
  margin-bottom: 50px;
}

.blog-posts__post {
  text-align: center;
}

.blog-posts__post-title {
  font-family: {{ settings.header_font | remove('Google+') | replace({'+': ' '}) }};
  color: {{ settings.title_color }};
}

.blog-posts__date {
  color: {{ settings.subtitle_color }};
}

/* - - - - */

.post__date {
  color: {{ settings.subtitle_color }};
}

/* * * * * * * */
/* Cart styles */
/* * * * * * * */

.cart__resume-table {}
.cart__resume-table th {
  color: {{ settings.title_color }};
  text-transform: uppercase;
}

.cart__resume-table__product-name {
  font-size: 18px;
  font-weight: 400;
  margin-top: 10px;
  margin-bottom: 10px;
  display: block;
  line-height: 1.1;
}
select.cart__resume-table__product-quantity {
  margin-top: 5px;
  width: 60px;
  height: 40px;
  border-radius: 0;
  border: none;
  border-bottom: 2px solid {{ settings.border_color }};
  display: inline-block;
  max-width: 70px;
  -webkit-appearance: none;
  cursor: pointer;
  float: left;
  background: url('{{ 'caret-down.png' | asset_url }}') 78% / 19% no-repeat {{ settings.product_card_bg_color }};
}

.cart__resume-table__product-amount {
  text-align: right;
  padding-top: 18px !important;
}

.cart__resume-table__product-remove {
  border: none;
  background-color: inherit;
  font-size: 25px;
  width: 100%;
  margin: 0 auto;
  color: #000;
}

.cart-totals {
  padding: 15px 20px;
}

a.btn.btn-share {
  color: {{ settings.body_font_color }};
  background-color: transparent;
  border: 1px solid {{ settings.border_color }};
  margin: 0 10px 10px 0;
}

a.btn.btn-share.share-facebook i {
  color: #3b5998;
}

a.btn.btn-share.share-facebook:hover {
  color: #fff;
  background-color: #2d4373;
}

a.btn.btn-share.share-twitter i {
  color: #00aced;
}

a.btn.btn-share.share-twitter:hover {
  color: #fff;
  background-color: #0087ba;
}

a.btn.btn-share.share-pinterest i {
  color: #cb2027;
}

a.btn.btn-share.share-pinterest:hover {
  color: #fff;
  background-color: #9f191f;
}

a.btn.btn-share.share-google i {
  color: #dd4b39;
}

a.btn.btn-share.share-google:hover {
  color: #fff;
  background-color: #c23321;
}

/* Slider */

.slide {
  position: relative;
}

.slide__caption {
  background-color: rgba(0,0,0,0.6);
  bottom: -10px;
  color: #fff;
  min-height: 80px;
  left: 0;
  padding: 20px;
  position: absolute;
  width: 100%;
  z-index: 2;
}

.slick-slider:hover .slick-arrow {
  display: block !important;
}

/* Arrows */
.slick-prev,
.slick-next {
  display: none !important;
  font-size: 6em;
  line-height: 0;

  position: absolute;
  top: 50%;

  padding: 0;
  -webkit-transform: translate(0, -50%);
  -ms-transform: translate(0, -50%);
  transform: translate(0, -50%);

  cursor: pointer;

  opacity: .55;
  color: black;
  border: none;
  outline: none;
  background: transparent;
  z-index: 3;
}

.slick-prev:hover,
.slick-prev:focus,
.slick-next:hover,
.slick-next:focus {
  opacity: .3;
  outline: none;
  background: transparent;
}

.slick-prev:hover:before,
.slick-prev:focus:before,
.slick-next:hover:before,
.slick-next:focus:before {
  opacity: 1;
}

.slick-prev.slick-disabled:before,
.slick-next.slick-disabled:before {
  opacity: .25;
}

.slick-prev {
  left: 0;
}

.slick-next {
  right: 0;
}
